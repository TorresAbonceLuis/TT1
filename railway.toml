[build]
builder = "NIXPACKS"
buildCommand = "cd Backend && pip install -r requeriment.txt"

[deploy]
startCommand = "cd Backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[nixpacks]
nixPkgs = ["python39", "pip"]